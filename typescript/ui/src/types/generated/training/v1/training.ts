// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               v6.33.4
// source: training/v1/training.proto

/* eslint-disable */

export const protobufPackage = "training.v1";

export interface ClearDataRequest {
  tenant_id: string;
}

export interface ClearDataResponse {
  success: boolean;
  tables_cleared: string[];
}

export interface TrainRequest {
  max_depth: number;
  training_window_days: number;
  /** Explicit list of feature names. */
  selected_feature_columns: string[];
  /** Versioned feature set identifier. */
  feature_set_id?:
    | string
    | undefined;
  /**
   * Feature groups to expand (e.g. "transaction", "user", "merchant", "network").
   * Case-insensitive, whitespace is stripped.
   */
  feature_groups: string[];
  /**
   * Resolution mode:
   * "strict" (default): Fail if any feature/group is unknown or group is empty.
   * "best_effort": Drop unknown features/groups and proceed with what's found.
   */
  feature_resolution_mode: string;
  split_config?: SplitConfig | undefined;
  tuning_config?: TuningConfig | undefined;
  n_estimators: number;
  learning_rate: number;
  min_child_weight: number;
  subsample: number;
  colsample_bytree: number;
  gamma: number;
  reg_alpha: number;
  reg_lambda: number;
  random_state: number;
  early_stopping_rounds?: number | undefined;
  min_cal_samples?: number | undefined;
  min_cal_pos?: number | undefined;
  min_cal_neg?: number | undefined;
  tenant_id: string;
}

export interface SplitConfig {
  /** temporal, temporal_stratified, etc. */
  strategy: string;
  n_folds: number;
  stratify_column?: string | undefined;
  group_column: string;
  validation_fraction: number;
  seed: number;
}

export interface TuningConfig {
  enabled: boolean;
  /** grid, random, bayesian */
  strategy: string;
  n_trials: number;
  timeout_minutes: number;
  metric: string;
  /** maximize, minimize */
  direction: string;
  selected_trial_number?:
    | number
    | undefined;
  /** JSON-encoded range, e.g. "[2, 12]" */
  search_space: { [key: string]: string };
}

export interface TuningConfig_SearchSpaceEntry {
  key: string;
  value: string;
}

export interface TrainResponse {
  success: boolean;
  run_id: string;
  status: string;
  model_version: string;
  error: string;
  tenant_id: string;
}

export interface GetTrainingRunInfoRequest {
  run_id: string;
  tenant_id: string;
}

export interface GetTrainingRunInfoResponse {
  /** JSON-serialized TrainingRunSpec */
  run_spec_json: string;
}

export interface GetModelInfoRequest {
  /** Optional: model name, defaults to production if empty. */
  model_name: string;
  tenant_id: string;
}

export interface GetModelInfoResponse {
  model_name: string;
  model_version: string;
  required_features: string[];
  feature_set_hash: string;
  training_config_hash: string;
  run_id: string;
}

export interface ValidateTrainRequestResponse {
  valid: boolean;
  resolved_features: string[];
  feature_set_hash: string;
  training_config_hash: string;
  warnings: string[];
}

export interface ListFeatureSetsRequest {
  limit: number;
  cursor: string;
  tenant_id: string;
}

export interface ListFeatureSetsResponse {
  feature_sets: ListFeatureSetsResponse_FeatureSetSummary[];
  next_cursor: string;
}

export interface ListFeatureSetsResponse_FeatureSetSummary {
  id: string;
  hash: string;
  feature_count: number;
  created_at: string;
  created_by: string;
}

export interface StartTuningJobResponse {
  job_id: string;
  status: string;
  mlflow_run_id: string;
}

export interface GetTuningStatusRequest {
  job_id: string;
  tenant_id: string;
}

export interface TuningJobStatusResponse {
  job_id: string;
  status: string;
  completed_trials: number;
  total_trials: number;
  pruned_trials: number;
  best_value: number;
  best_params: { [key: string]: string };
  mlflow_run_id: string;
  error_message: string;
  created_at: string;
  started_at: string;
  updated_at: string;
  ended_at: string;
  heartbeat_at: string;
}

export interface TuningJobStatusResponse_BestParamsEntry {
  key: string;
  value: string;
}

export interface ListTuningJobsRequest {
  statuses: string[];
  limit: number;
  cursor: string;
  tenant_id: string;
}

export interface TuningJobSummary {
  job_id: string;
  status: string;
  created_at: string;
  started_at: string;
  updated_at: string;
  ended_at: string;
  mlflow_run_id: string;
  total_trials: number;
  completed_trials: number;
  pruned_trials: number;
  best_value?: number | undefined;
  metric: string;
  direction: string;
  requested_by?: string | undefined;
  error_message?: string | undefined;
}

export interface ListTuningJobsResponse {
  jobs: TuningJobSummary[];
  next_cursor: string;
}

export interface GetQueueDepthRequest {
  tenant_id: string;
}

export interface GetQueueDepthResponse {
  depth: number;
}

export interface PromoteTrialRequest {
  job_id: string;
  trial_number: number;
  model_name: string;
  dry_run: boolean;
  tenant_id: string;
}

export interface PromoteTrialResponse {
  status: string;
  mlflow_run_id: string;
  model_version: string;
  error_message: string;
}

export interface GetTuningJobInfoRequest {
  job_id: string;
  trials_limit: number;
  trials_cursor: string;
  sort_by: string;
  tenant_id: string;
}

export interface TrialResult {
  trial_number: number;
  state: string;
  value: number;
  params: { [key: string]: string };
  started_at: string;
  ended_at: string;
  duration_ms: number;
}

export interface TrialResult_ParamsEntry {
  key: string;
  value: string;
}

export interface GetTuningJobInfoResponse {
  job?: TuningJobSummary | undefined;
  trials: TrialResult[];
  next_cursor: string;
  mlflow_links: { [key: string]: string };
}

export interface GetTuningJobInfoResponse_MlflowLinksEntry {
  key: string;
  value: string;
}

export interface ListTrialsRequest {
  job_id: string;
  limit: number;
  /** "value" or "trial_number" */
  sort_by: string;
  tenant_id: string;
  cursor: string;
}

export interface TrialRecord {
  trial_number: number;
  state: string;
  value: number;
  params: { [key: string]: string };
  started_at: string;
  ended_at: string;
  duration_ms: number;
}

export interface TrialRecord_ParamsEntry {
  key: string;
  value: string;
}

export interface ListTrialsResponse {
  trials: TrialRecord[];
  next_cursor: string;
}

export interface CancelTuningJobRequest {
  job_id: string;
  tenant_id: string;
}

export interface RequeueTuningJobRequest {
  job_id: string;
  tenant_id: string;
}

export interface RequeueTuningJobResponse {
  job_id: string;
  status: string;
  message: string;
}

export interface FinalizeTuningJobRequest {
  job_id: string;
  final_status: string;
  reason: string;
  tenant_id: string;
}

export interface FinalizeTuningJobResponse {
  job_id: string;
  status: string;
  error_message: string;
}

export interface GetHealthRequest {
}

export interface GetHealthResponse {
  ready: boolean;
  components: { [key: string]: string };
  spooled_reports_count: number;
}

export interface GetHealthResponse_ComponentsEntry {
  key: string;
  value: string;
}

export interface TrainingService {
  /** Clears all data from the database. */
  ClearData(request: ClearDataRequest): Promise<ClearDataResponse>;
  /** Initiates model training. */
  Train(request: TrainRequest): Promise<TrainResponse>;
  /** Fetches metadata for a specific training run. */
  GetTrainingRunInfo(request: GetTrainingRunInfoRequest): Promise<GetTrainingRunInfoResponse>;
  /** Fetches info about the active production model. */
  GetModelInfo(request: GetModelInfoRequest): Promise<GetModelInfoResponse>;
  /** Validates a training request without executing training. */
  ValidateTrainRequest(request: TrainRequest): Promise<ValidateTrainRequestResponse>;
  /** Lists all registered feature sets. */
  ListFeatureSets(request: ListFeatureSetsRequest): Promise<ListFeatureSetsResponse>;
  /** Starts an asynchronous hyperparameter tuning job. */
  StartTuningJob(request: TrainRequest): Promise<StartTuningJobResponse>;
  /** Checks the status of a tuning job. */
  GetTuningStatus(request: GetTuningStatusRequest): Promise<TuningJobStatusResponse>;
  /** Lists tuning jobs for operator visibility. */
  ListTuningJobs(request: ListTuningJobsRequest): Promise<ListTuningJobsResponse>;
  /** Returns current in-memory queue depth. */
  GetQueueDepth(request: GetQueueDepthRequest): Promise<GetQueueDepthResponse>;
  /** Retrains and registers a model using parameters from a tuning trial. */
  PromoteTrial(request: PromoteTrialRequest): Promise<PromoteTrialResponse>;
  /** Returns a unified tuning job view including summary and trial page. */
  GetTuningJobInfo(request: GetTuningJobInfoRequest): Promise<GetTuningJobInfoResponse>;
  /** Lists trials for a specific tuning job. */
  ListTrials(request: ListTrialsRequest): Promise<ListTrialsResponse>;
  /** Cancels a running tuning job. */
  CancelTuningJob(request: CancelTuningJobRequest): Promise<TuningJobStatusResponse>;
  /** Requeues a terminal tuning job for another execution attempt. */
  RequeueTuningJob(request: RequeueTuningJobRequest): Promise<RequeueTuningJobResponse>;
  /** Force-finalizes a tuning job with an operator-provided reason. */
  FinalizeTuningJob(request: FinalizeTuningJobRequest): Promise<FinalizeTuningJobResponse>;
  /** Fetches health status including spool information. */
  GetHealth(request: GetHealthRequest): Promise<GetHealthResponse>;
}
